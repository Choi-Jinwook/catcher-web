import { ScheduleCard } from "@shared/components";
import { templateContent } from "../constants";

const Template = () => {
  return <ScheduleCard content={templateContent} callType="template" />;
};

export default Template;
